---
micronaut:
  server:
    port: 8082
  application:
    name: service-ac

---
consul:
  client:
    registration:
      enabled: true
    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
